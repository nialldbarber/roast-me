import { useState } from 'react'

const useAddStore = () => {
  const [store, setStore] = useState({
    name: '',
    description: '',
    rating: '',
  })

  // handle change

  // handle submit
}

export default useAddStore
