import React, { FC } from 'react'
// Types
import { Props, LikeProps } from '~@components/likes/types'

const Likes: FC<Props> = ({ likes }) => (
	<div>
		<p>Likes</p>
	</div>
)

export default Likes
