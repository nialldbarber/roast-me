import styled from 'styled-components'

export const Wrapper = styled.div`
	position: absolute;
	right: .5rem;
	bottom: .7rem;

	svg {
		width: 50px;
		height: auto;
	}

	span {
		position: absolute;
		left: 50%;
		bottom: 50%;
		transform: translate(-50%, 55%);
	}
`
